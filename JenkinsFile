pipeline{
    agent any
    triggers{
        pollSCM('* * * * *')
    }

    stages{
        stage("Unit Testing")
        {
            steps { 
                sh "echo 'mvn test', pwd "
                sh "ls"
             }
        }
    }
}
